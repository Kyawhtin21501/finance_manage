generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Todo {
  id        String   @id @default(cuid())
  content   String
  createdAt DateTime @default(now())
}
